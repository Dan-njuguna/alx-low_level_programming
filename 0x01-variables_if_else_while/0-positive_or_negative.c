#include <stdio.h>
/**
 * main - entry
 * Return: Always 0
 */
int main(void)
{
	int n;
	if (n>0);
	printf("is positive\n");
	if (n=0);
	printf("is zero\n");
	else{ printf("is negative\n");}
	return (0);
}
