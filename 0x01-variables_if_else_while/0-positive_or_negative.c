#include <stdio.h>
/**
 * main - entry
 * Return: Always 0
 */
int main(void)
{
	int n = 0;
	n++;
	n--;

	if
		(n > 0)
	{
		printf("%d is positive\n", n);
	}
	if
		(n == 0)
	{
		printf("%d is zero\n", n);
	}
	else
	{
		printf("%d is negative\n", n);
	}
	return (0);
}
