#include <stdio.h>
/**
 * main - prints if a number is positive, negative or 0i
 * Return: Always 0
 */
int main(void)
{
	int n;
	/*Positive if number is greater than 0*/
	if
		(n > 0);
	{
		printf("%d is positive \n", n);
	} else if
		(n == 0);
	{
		printf("%d is zero \n", n);
	} else
	printf("%d is negative \n", n);
	return (0);
}
